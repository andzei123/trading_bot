# E26 Power-Loss Summary

External hard-power-off validation has not been executed.

```text
EXTERNAL HARD-POWER-OFF VALIDATION:
NOT EXECUTED — REPOSITORY OWNER ACTION REQUIRED
```

The factual summary must be regenerated from `E26_1_POWER_LOSS_RESULTS.jsonl` with `tools/e26_power_loss_summary.py`. It may report PASS only when every required phase has the configured minimum iterations, forbidden outcomes equal zero, and verification errors equal zero.
